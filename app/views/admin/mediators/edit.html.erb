<%= render Admin::Pages::AdminPageComponent.new(
          breadcrumbs: Admin::Pages::Breadcrumbs::BreadcrumbComponent.new(
            links: [
              Admin::Pages::Breadcrumbs::Links::LinkComponent.new(
                url: admin_mediators_path,
                text: t('admin.component.breadcrumbs.mediators'))
            ]
          )) do %>

  <%= render 'form', title: t('admin.page.mediators.edit.title'), path: admin_mediator_path %>

<% end %>
