<% items.each do |item| %>
  <%= render Admin::Tables::Items::SimpleRowComponent.new(
              resource: item,
              fields: [
                Admin::Tables::Fields::TextComponent.new(item.id),
                Admin::Tables::Fields::TextComponent.new(item.admin_user),
                Admin::Tables::Fields::TextComponent.new(Case.find(item.case_id)),
                Admin::Tables::Fields::TextComponent.new(item.party),
                Admin::Tables::Fields::TextComponent.new(item.subject),
              ],
              actions: [
                # Admin::Tables::Actions::IconButtonComponent.new(url: edit_admin_message_path(item), icon: 'la la-pencil'),
                # Admin::Tables::Actions::DestroyButtonComponent.new(url: admin_message_path(item))
              ]) %>
<% end %>
